{
  "name": "streamflix",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm rebuild better-sqlite3",
    "start": "cd backend && node server.js"
  }
}